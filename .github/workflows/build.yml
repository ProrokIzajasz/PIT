name: build
on: [push, pull_request]
jobs:
  windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x
      - run: dotnet restore PIT.csproj
      - run: dotnet build PIT.csproj -c Release --no-restore
      - run: dotnet test PIT.Tests/PIT.Tests.csproj -c Release
